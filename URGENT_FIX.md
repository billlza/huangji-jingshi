# ⚠️ 紧急修复 - 必须手动修改 Start Command

## 🚨 问题原因

Render 不会自动从 yaml 文件更新已有服务的配置！
必须在 Dashboard 中手动修改。

---

## ✅ 5步修复（2分钟）

### 1️⃣ 打开 Settings
在 Render Dashboard 左侧菜单点击 **"Settings"**

### 2️⃣ 找到 Start Command
向下滚动，找到 **"Start Command"** 配置框

**当前的错误配置：**
```
cd backend && ./target/release/backend
```

### 3️⃣ 修改配置
**删除所有内容，替换为：**
```
./target/release/backend
```

**重要：** 
- ✅ 正确：`./target/release/backend`
- ❌ 错误：`cd backend && ./target/release/backend`

### 4️⃣ 保存
点击 **"Save Changes"** 按钮

### 5️⃣ 重新部署
- 点击右上角 **"Manual Deploy"**
- 选择 **"Deploy latest commit"**
- 等待 1-2 分钟

---

## 🎯 为什么必须这样改？

**错误的命令：**
```bash
cd backend && ./target/release/backend
```
1. 执行 `cd backend` → 进入 `huangji-jingshi-web/backend/` 目录
2. 执行 `./target/release/backend` → 找 `huangji-jingshi-web/backend/target/release/backend`
3. ❌ 但文件在 `huangji-jingshi-web/target/release/backend`
4. ❌ 找不到文件！

**正确的命令：**
```bash
./target/release/backend
```
1. 直接在 `huangji-jingshi-web/` 目录执行
2. ✅ 直接运行 `huangji-jingshi-web/target/release/backend`
3. ✅ 文件存在，成功！

---

## 📸 对照图

**Settings 页面应该找到这个：**
```
┌─────────────────────────────────────────┐
│ Start Command                            │
│ ┌─────────────────────────────────────┐ │
│ │ cd backend && ./target/release/bac…│ │  ← 删除这个
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘

改成：

┌─────────────────────────────────────────┐
│ Start Command                            │
│ ┌─────────────────────────────────────┐ │
│ │ ./target/release/backend           │ │  ← 改成这个
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

---

## ✅ 成功标志

修改后重新部署，日志会显示：
```
==> Running './target/release/backend'
🚀 皇极经世后端服务启动中...
🌐 启动服务器，端口: 10000
```

**不会再出现：**
```
bash: line 1: ./target/release/backend: No such file or directory  ← 这个错误
```

---

## 🔍 验证

部署成功后测试：
```bash
curl https://hjjs-backend.onrender.com/health
```

**预期响应：**
```json
{
  "status": "ok",
  "message": "皇极经世后端服务正常运行"
}
```

---

## 🆘 如果还是找不到配置

如果在 Settings 找不到 Start Command：

1. 确认你在正确的服务页面（hjjs-backend 或 hjjs-backend-simple）
2. Settings 向下滚动到 "Build & Deploy" 区域
3. 应该能看到：
   - Build Command
   - Start Command  ← 在这里改
   - Health Check Path

---

**立即去修改吧！就改一行，2分钟搞定！** 🚀

