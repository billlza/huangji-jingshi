services:
  - type: web
    name: hjjs-backend
    env: rust
    plan: free
    buildCommand: cargo build --release --bin backend
    startCommand: ./target/release/backend
    envVars:
      - key: RUST_LOG
        value: info
      - key: PORT
        sync: false
